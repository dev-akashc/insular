<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" android:maxSdkVersion="25" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_PROFILES" />
    <uses-permission android:name="android.permission.GET_APP_OPS_STATS" tools:ignore="ProtectedPermissions" /> <!-- AppOpsCompat -->
    <uses-permission android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application android:name="com.akash.island.IslandApplication" tools:ignore="AllowBackup,GoogleAppIndexingWarning,MissingApplicationIcon">

        <meta-data android:name="firebase_crashlytics_collection_enabled" android:value="false" />

        <provider android:name="com.akash.island.util.Users"
                  android:initOrder="1000"
                  android:authorities="${applicationId}.users"
                  android:directBootAware="true"
                  android:exported="false" />

        <activity android:name="com.akash.island.shuttle.ShuttleCarrierActivity"
                  android:theme="@android:style/Theme.Translucent.NoTitleBar"
                  android:directBootAware="true"
                  android:exported="false">
            <intent-filter>
                <action android:name="com.akash.island.action.SHUTTLE" />
                <category android:name="com.akash.island.category.PARENT_PROFILE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <provider android:authorities="${applicationId}.shuttle" android:exported="false"
                  android:name="com.akash.island.shuttle.ShuttleProvider" android:grantUriPermissions="true"
                  android:directBootAware="true" />

    </application>

</manifest>
